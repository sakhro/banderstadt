import React from "react";
import NextHead from "next/head";

export const Head: React.FC = (props) => (
  <NextHead>
    <title>BANDERSTAD</title>
    <meta name="description" content="Glory for Ukraine" />
  </NextHead>
);
